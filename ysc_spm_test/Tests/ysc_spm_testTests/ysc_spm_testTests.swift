import XCTest
@testable import ysc_spm_test

final class ysc_spm_testTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ysc_spm_test().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
